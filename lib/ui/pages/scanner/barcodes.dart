import 'package:flutter/material.dart';

class BarcodeSummary extends StatelessWidget {
  const BarcodeSummary({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
